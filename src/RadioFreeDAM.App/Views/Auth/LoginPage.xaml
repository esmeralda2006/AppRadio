<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:auth="clr-namespace:RadioFreeDAM.App.ViewModels.Auth"
             x:Class="RadioFreeDAM.App.Views.Auth.LoginPage"
             x:DataType="auth:LoginViewModel"
             Title="Login"
             Shell.NavBarIsVisible="False">
    
    <ScrollView>
        <VerticalStackLayout Padding="30" Spacing="20" VerticalOptions="Center">
            
            <Label 
                Text="RadioFreeDAM"
                Style="{StaticResource HeaderLabel}"
                Margin="0,0,0,40"/>
            
            <Label Text="Usuario" Style="{StaticResource SecondaryLabel}" Margin="5,0"/>
            <Frame Style="{StaticResource DarkFrame}">
                <Entry 
                    Text="{Binding Username}"
                    Placeholder="Tu nombre de usuario"
                    Style="{StaticResource DarkEntry}"/>
            </Frame>
            
            <Label Text="Contraseña" Style="{StaticResource SecondaryLabel}" Margin="5,0"/>
            <Frame Style="{StaticResource DarkFrame}">
                <Grid ColumnDefinitions="*,Auto">
                    <Entry 
                        Grid.Column="0"
                        Text="{Binding Password}"
                        Placeholder="********"
                        IsPassword="{Binding IsPasswordMasked}"
                        Style="{StaticResource DarkEntry}"/>
                    <Button
                        Grid.Column="1"
                        Text="👁"
                        FontSize="18"
                        Padding="10,0"
                        BackgroundColor="Transparent"
                        TextColor="{StaticResource TextGray}"
                        Command="{Binding TogglePasswordCommand}"
                        WidthRequest="40"
                        HeightRequest="40"/>
                </Grid>
            </Frame>
            
            <ActivityIndicator 
                IsRunning="{Binding IsLoading}"
                IsVisible="{Binding IsLoading}"
                Color="{StaticResource Primary}"
                HorizontalOptions="Center"/>

            <Button 
                Text="Iniciar Sesión"
                Style="{StaticResource PrimaryButton}"
                Margin="0,20,0,0"
                Command="{Binding LoginCommand}"
                IsEnabled="{Binding IsLoading, Converter={StaticResource InvertedBoolConverter}}"/>
            
            <HorizontalStackLayout HorizontalOptions="Center" Spacing="5" Margin="0,20,0,0">
                <Label 
                    Text="¿No tienes cuenta?"
                    Style="{StaticResource SecondaryLabel}"/>
                <Label 
                    Text="Regístrate"
                    TextColor="{StaticResource Primary}"
                    FontSize="14"
                    FontAttributes="Bold">
                    <Label.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding GoToRegisterCommand}"/>
                    </Label.GestureRecognizers>
                </Label>
            </HorizontalStackLayout>
            
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
